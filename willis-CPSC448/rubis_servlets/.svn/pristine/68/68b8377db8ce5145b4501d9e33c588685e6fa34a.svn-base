package remoting;

public class RemotingConstants {
	
	public static final String REMOTE_URL = System.getProperty("remote.host");
	public static final String REMOTE_PORT = System.getProperty("remote.port");
	public static final String URL = "http://" + REMOTE_URL + ":" + REMOTE_PORT + "/JForum2WebPremise/Remoting";
	
}

